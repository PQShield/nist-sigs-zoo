{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git diff --name-only HEAD 2>/dev/null | grep -qE '^(src/|data/schemes/)' && echo '{\"systemMessage\": \"Remember to update data/history.yaml if changes are significant. Commit and add the commit url beforehand if it makes sense to link to the commit.\"}' || true"
          }
        ]
      }
    ]
  },
  "enabledPlugins": {
    "frontend-design@claude-plugins-official": true
  }
}
